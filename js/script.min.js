"use strict";

var inputs = document.querySelectorAll(".vlogers__select-input"),
    options = document.querySelectorAll(".vlogers__option"),
    optionsItem = document.querySelectorAll(".vlogers__option-item");
var open = !1;
document.body.addEventListener("click", function (e) {
  "open" === e.target.dataset.open || e.target.classList.contains("disabled") ? e.target.classList.contains("disabled") ? e.target.parentNode.parentNode.classList.add("_active") : (open = !open, document.querySelectorAll('[data-dropdown="dropdown"]').forEach(function (t) {
    t.classList.remove("_active"), t.dataset.target === e.target.id && open ? t.classList.add("_active") : t.classList.remove("_active");
  })) : document.querySelectorAll('[data-dropdown="dropdown"]').forEach(function (e) {
    e.classList.remove("_active");
  });
}), options.forEach(function (e) {
  e.addEventListener("click", function (t) {
    inputs.forEach(function (o) {
      t.target.classList.contains("disabled") ? e.classList.add("_active") : o.id === e.dataset.target && (o.value = t.target.innerHTML, o.dataset.value = t.target.dataset.value, open = !1);
    });
  });
});
var headerTop = document.querySelector(".header__top"),
    nav = document.querySelector(".header__top-nav"),
    languages = document.querySelector(".header__top-lang"),
    burger = document.querySelector(".header__burger");
window.addEventListener("resize", function (e) {
  addChild(window.innerWidth);
});
var windowInnerWidth = window.innerWidth;

function addChild(e) {
  e <= 768 ? nav.appendChild(languages) : headerTop.appendChild(languages);
}

if (addChild(windowInnerWidth), burger && burger.addEventListener("click", function () {
  burger.classList.toggle("_openMenu"), burger.classList.contains("_openMenu") ? (nav.classList.add("_open"), document.body.classList.add("_lock")) : (nav.classList.remove("_open"), document.body.classList.remove("_lock"));
}), "vloger" === document.body.dataset.init) {
  var e = document.createElement("li");
  e.innerHTML = '<a href="/" class="header__lower-breadcrumbs__link">Максим Максимов</a>', document.querySelector(".header__lower-breadcrumbs").appendChild(e);
}

var play = document.querySelectorAll(".video__play"),
    cover = document.querySelectorAll(".video__cover"),
    videos = document.querySelectorAll(".videos__container");

if (videos.length > 0) {
  var _loop = function _loop(_e) {
    videos[_e].addEventListener("click", function (t) {
      t.target.classList.contains("video__play") && (cover[_e].classList.add("_remove"), videos[_e].classList.toggle("_play"));
    });
  };

  for (var _e = 0; _e < videos.length; _e++) {
    _loop(_e);
  }
}

var slider = new Swiper(".others__slider", {
  pagination: {
    el: ".swiper-pagination",
    type: "progressbar"
  },
  navigation: {
    nextEl: ".swiper-button-next",
    prevEl: ".swiper-button-prev"
  },
  spaceBetween: 10,
  breakpoints: {
    320: {
      slidesPerView: 1
    },
    576: {
      slidesPerView: 2
    },
    992: {
      slidesPerView: 3
    },
    1298: {
      slidesPerView: 4
    }
  }
});